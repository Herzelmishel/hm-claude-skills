[pytest]
testpaths = preseed_campaign preseed_voice preseed_prospect preseed_outreach preseed_pipeline
python_files = test_*.py
addopts = -ra -q --tb=short
filterwarnings =
    ignore::DeprecationWarning:pkg_resources
